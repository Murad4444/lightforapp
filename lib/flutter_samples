// 1. Объявляем функцию (define)
// Она должна быть ВНЕ метода build, но внутри класса
void myAction() {
  print('Логика выполнена отдельно!');
}

@override
Widget build(BuildContext context) {
  return ElevatedButton(
    style: ElevatedButton.styleFrom(backgroundColor: Colors.red),
    
    // 2. Подключаем её по имени БЕЗ скобок
    onPressed: myAction, 
    
    child: Text('Красная кнопка'),
  );
}






ElevatedButton(
  style: ElevatedButton.styleFrom(
    backgroundColor: Colors.red, // Цвет кнопки
    foregroundColor: Colors.white, // Цвет текста/иконки
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(20), // Скругление углов
    ),
  ),
  onPressed: () {},
  child: Text('Красная кнопка'),
)